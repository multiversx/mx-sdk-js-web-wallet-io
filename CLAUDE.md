# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**See [AGENTS.md](./AGENTS.md).** It is the canonical guide for agents working in this repo — setup, commands, architecture, the invariants that must not be broken, testing conventions, and the verification checklist to run before calling a change done.

Everything an agent needs lives there so there is a single source of truth. Do not duplicate that content here; extend `AGENTS.md` instead.
