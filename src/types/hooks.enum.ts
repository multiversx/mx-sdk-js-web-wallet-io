export enum HookSearchParamsEnum {
  executeAfterSign = 'executeAfterSign',
  modal = 'modal'
}

export enum HookResponseStatusEnum {
  success = 'success',
  failed = 'failed',
  cancelled = 'cancelled'
}
